<Project>
	
	<Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)..\'))" />
	
    <PropertyGroup>
		<OutDir>$(SolutionDir)\build\src\$(Configuration)\$(TargetFramework)</OutDir>
    </PropertyGroup>

</Project>
